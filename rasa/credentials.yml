# Messaging channel credentials. Use environment variables in production.

# Twilio WhatsApp (recommended for quick start via Sandbox)
twilio:
  account_sid: ${TWILIO_ACCOUNT_SID}
  auth_token: ${TWILIO_AUTH_TOKEN}
  # For WhatsApp use the sandbox number like: whatsapp:+14155238886
  twilio_number: ${TWILIO_NUMBER}

# Example: Facebook/WhatsApp Cloud API (Meta Business) [alternative to Twilio]
#facebook:
#  verify: ${FB_VERIFY_TOKEN}
#  secret: ${FB_APP_SECRET}
#  page-access-token: ${FB_PAGE_ACCESS_TOKEN}

# REST channel for web clients / testing
rest:

# Enable socketio webchat if needed (frontend widget)
#socketio:
#  user_message_evt: user_uttered
#  bot_message_evt: bot_uttered
#  session_persistence: true
